apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: resume-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
  - host: localhost
    http:
      paths:
      - pathType: Prefix
        path: /api/(.*)
        backend:
          service:
            name: resume-backend
            port:
              number: 5000
      - pathType: Prefix
        path: /
        backend:
          service:
            name: resume-frontend
            port:
              number: 80
