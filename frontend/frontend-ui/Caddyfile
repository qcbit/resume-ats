:80 {
    root * /usr/share/caddy
    file_server
    try_files {path} /index.html # Handles SPA routing
    log {
        output stdout # Send access logs to stdout
        format console # Use a simple console format
    }
}